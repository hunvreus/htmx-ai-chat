# Step 8 - Markdown Rendering

Concept: keep Step 7 flow and render assistant messages as markdown on the client (including SSE updates), with text-only chat input.

Run:

```bash
uv run python run.py --step 8
```
