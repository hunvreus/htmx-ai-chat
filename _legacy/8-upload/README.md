# Step 8 - Upload

Concept: start from Step 7 and add attachment support to user messages.
For image files, the API receives in-band file content (data URL), not a remote URL reference.

Run:

```bash
uv run python run.py --step 8
```
