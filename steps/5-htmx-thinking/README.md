# Step 5 - HTMX Thinking Placeholder

Concept: return user message + assistant `Thinking...` immediately, then replace placeholder when answer is ready.

Run:

```bash
uv run python run.py --step 5
```
