# Step 6 - SSE Streaming

Concept: keep the Step 5 UI/flow and replace polling with true token streaming via SSE, using the official HTMX SSE extension (`hx-ext="sse"`).

Run:

```bash
uv run python run.py --step 6
```
