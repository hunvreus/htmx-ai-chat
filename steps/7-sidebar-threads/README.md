# Step 7 - Sidebar + Threads

Concept: add UUID thread routes and a Basecoat sidebar, with HTMX `hx-boost` navigation that swaps the main chat content in place.

Run:

```bash
uv run python run.py --step 7
```
